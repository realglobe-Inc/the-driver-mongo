/**
 * Test for mongoMix.
 * Runs with mocha.
 */
'use strict'

const mongoMix = require('../lib/mixins/mongoMix')
const { ok, equal } = require('assert')

describe('mongo-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
