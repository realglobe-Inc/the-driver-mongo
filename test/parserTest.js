/**
 * Test for parser.
 * Runs with mocha.
 */
'use strict'

const parser = require('../lib/helpers/parser')
const { ok, equal } = require('assert')

describe('parser', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
