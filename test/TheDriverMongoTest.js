/**
 * Test for TheDriverMongo.
 * Runs with mocha.
 */
'use strict'

const TheDriverMongo = require('../lib/TheDriverMongo')
const { ok, equal } = require('assert')

describe('the-driver-mongo', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(TheDriverMongo)
  })
})

/* global describe, before, after, it */
